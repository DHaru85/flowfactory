# 2026-08-20 工作流 Studio 画布 - 表示层 - 修改

状态：**unreached**。待模型/配置/会话前端首轮验收后再移入 `docs/plan/`。

## 用户需求理解

Studio 应用：Flow v1 草稿/发布/新草稿，画布读写 `definition.view`，目录只读 Profile/LLM/Tool。

## 需求中的待澄清事项

无待澄清事项。须首轮前端已验收。

## 施工方案

1. 对接 `/api/v1/studio/*`；图编辑器只把坐标写入 `view`，拓扑走 nodes/edges/branches。
2. 发布前客户端可做基本校验，最终以服务端 `definition_invalid` 为准。

## 方案待澄清事项

无待澄清事项。

## 验收标准

1. 可创建 draft、保存、发布、基于 published 开新草稿。
2. `npm run build` 通过。
