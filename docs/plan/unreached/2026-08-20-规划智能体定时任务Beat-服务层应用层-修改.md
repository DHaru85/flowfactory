# 2026-08-20 规划智能体定时任务 Beat - 服务层/应用层 - 修改

状态：**unreached**。第 1 轮 Beat **仅绑工作流**；本文件覆盖规划侧定时。移入 `docs/plan/` 并确认后开工。

## 用户需求理解

`agent_beat_task` 除 Flow 外可绑定 Profile，到期触发自研规划循环入队（系统用户 `beat_system_user_id`）。

## 需求中的待澄清事项

无待澄清事项。须第 3 轮规划循环已落地（`PlannerRuntime` / `kind=planner`）。

## 施工方案

1. Alembic：`flow_id` 改为可空；新增可空 `profile_id` FK → `agent_profile`；Check 二者恰一。
2. `dispatch_due_tasks`：有 `flow_id` 保持 `WorkflowRuntimeService.start`；有 `profile_id` 入队规划 Run。锁与 croniter 不变。
3. `agent_config` 增加规划 Beat HTTP，或与工作流 Beat 共用资源但互斥字段。
4. 不改第 1 轮已有「仅 flow_id」客户端契约时，须版本化字段（缺省仍为工作流）。
5. 单测区分两种触发。

## 方案待澄清事项

无待澄清事项。

## 验收标准

1. 恰一约束在库层生效。
2. Profile Beat 不启动 Flow compile。
3. ruff 通过；`data_schema_server.md` 同步。
