# 2026-08-20 自主规划与模型管理前端 - 表示层 - 修改

状态：**unreached**。后端第 1 轮起的配置/模型/会话/规划能力就绪后再开工。移入 `docs/plan/` 并确认。

## 用户需求理解

表示层提供模型管理、Agent 配置（Profile/技能/工具/MCP/工作流 Beat）、规划会话与工作流会话（Ant Design + Ant Design X），对接已有 SSE。

## 需求中的待澄清事项

无待澄清事项。须第 1 轮 API 已验收；会话前缀与规划循环按当时已落地轮次对接。

## 施工方案

1. React + Vite；不引入 Vercel AI SDK。
2. 页面：`models` LLM 表单（密钥只写不回显）；`agent_config` 配置与绑定编辑（绑定 UI 可先做，审核以后端 RBAC 轮为准）。
3. 会话：planner / workflow 分入口，共用消息组件与 SSE。
4. Axios 统一 401/403。
5. `npm run build` 通过。

## 方案待澄清事项

无待澄清事项。

## 验收标准

1. 构建成功；登录后可打开配置页与会话页。
2. 密钥输入框不回显明文。
