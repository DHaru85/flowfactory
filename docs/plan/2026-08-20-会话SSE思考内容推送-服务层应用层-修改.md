# 2026-08-20 会话 SSE 思考内容推送 - 服务层应用层 - 修改

## 用户需求理解

会话 SSE 流式输出未推送模型**思考 / 推理**内容。表示层已订阅 `event: reasoning`，设计文档已定义 `data.delta` + `data.message_id` 与 `content_blocks.type=reasoning`，但运行时只把 `delta.content` 打成 `speaking`。

现网模型为 Qwen3.5 类（`qwen35_122b_a10b`），思考一般在 OpenAI 兼容增量的 `reasoning_content` / `reasoning`，或正文里的 `<think>…</think>`。二者都未进入总线，完成后 `content_blocks` 也只有 `text`。

## 需求中的待澄清事项

无待澄清事项

## 施工方案

1. **LLM 流**：`stream_parts` 产出 `speaking` / `reasoning` 增量。读取 `reasoning_content`、`reasoning`；对 `content` 做 `<think>` 分段，思考不混入 `speaking`。`complete()` 只拼接面向用户的正文。
2. **投递**：规划节点与 workflow `llm` 节点按 kind 发 `reasoning` / `speaking`。`ChatTurn` 与图 `variables.last_reasoning` 保存完整思考。
3. **落库**：Run 完成时 `content_blocks` 为可选 reasoning 块 + text 块。
4. **SSE**：状态机已把 `reasoning` 当 delta，不改协议。表示层已拼接 `reasoning` 帧，本轮不改前端。
5. **测试**：Fake 可注入思考增量；断言总线 `reasoning` 事件与 think 标签拆分。

## 方案待澄清事项

无待澄清事项
