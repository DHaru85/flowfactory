# 2026-08-18 ORM 与 PG 数据层 - 映射层/数据层 - 修改

## 用户需求理解

用户要求按先前建议，优先实现 Permission、Conversation、Workflow 三域 ORM 模型及 PostgreSQL 数据层（连接池、事务管理），并连接已有 PG 实例（192.168.129.53:5432，库名 flowfactory，不存在则创建）。

## 待澄清事项

无待澄清事项。

## 施工方案

1. 搭建 `src/backend` 骨架：`settings`、`data_schema`、`service/database`。
2. 使用 SQLAlchemy 2.0 + psycopg（async 引擎 + sync 引擎供 Alembic）。
3. 实现三域 ORM：`sys_*`（Permission）、`conv_*`（Conversation）、`wf_*`（Workflow）。
4. Alembic 初始迁移；脚本创建 `flowfactory` 库并执行 `upgrade head`。
5. 单元测试：验证引擎连接与 metadata 可创建表。

## 方案待澄清事项

无待澄清事项（方案锁定）。

## 验收标准

1. 后端相关文件无 lint error。
2. 成功连接 PG，`flowfactory` 库存在且三域表已创建。
